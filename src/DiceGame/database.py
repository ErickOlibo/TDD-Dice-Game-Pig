import pickle


class Database:
    
    
    #   1 - Load Games from the Games_DB
    #       -> Games are unique by the UUID or name. 
    #       -> The data structure should be a Dictionary 
    #           * Keys: UUID (names)
    #           * Values: Game objects
    
    #   2 - Winners List in the database Winners_DB
    #       -> list of Tuple (Winner name, score, loser name, game_name) - order will de 
    #       -> Generate Highscore_List from winners list()
    #           * 
   
    pass